# Flask 1.1.0 -> 2.0.0 (221 first-parent commits).
# Documented breaking changes: drop Python 2 (#3554), remove simplejson
# (#3555/#3562), send_file moved to werkzeug wrappers (#3828). All three
# culprits below are Merge commits named in Flask's 2.0.0 changelog.
NAME=flask
URL=https://github.com/pallets/flask.git
OLD=1b4ace9ba5e77679bf9d8e409283654f7589907e   # tag 1.1.0
NEW=2f0c62f5e6e290843f03c1fa70817c7a3c7fd661   # tag 2.0.0

seed_beliefs() {
    # Evidence lines verified against the pinned 1.1.0 tree.
    belief cd8a3745047a4413387f1e8a6877fd35859e21be "changelog: drop Python 2 (#3554)" \
        "Flask supports Python 2 via flask._compat shims" \
        --evidence src/flask/helpers.py:35-35
    belief e69b49bd3d2df261316c1c5dbdd53b96d93d2f56 "changelog: JSON no longer uses simplejson (#3555, #3562)" \
        "flask.json prefers simplejson when it is installed" \
        --evidence src/flask/json/__init__.py:196-196
    belief bbb273bb761461ab329f03ff2d9002f6cb81e2a4 "changelog: send_file wraps werkzeug.utils implementation (#3828)" \
        "send_file builds its response inside Flask itself" \
        --evidence src/flask/helpers.py:642-644
    # Control: the deferred-registration lambda survives 1.1.0 -> 2.0.0
    # (it drifts from line 294 to 369 — drift, not staleness).
    belief SURVIVES "unchanged through 2.0.0 (drifts 294->369)" \
        "Blueprint.add_url_rule defers registration via record(lambda ...)" \
        --evidence src/flask/blueprints.py:294-294
}
