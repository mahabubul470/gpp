# zod v3.0.0 -> v4.0.1 (1237 first-parent commits).
# Two verdict flavors: a surgical in-v3 fix (#483 widens the uuid regex to
# admit the nil UUID) and the mass invalidation at the "Zod 4" merge, where
# the whole v3 tree (src/*) is replaced by the packages/ monorepo — the
# moment every v3 file-anchored fact genuinely stopped being true.
NAME=zod
URL=https://github.com/colinhacks/zod.git
OLD=1b1108030c2116620117af7e238aed50c23fc186   # tag v3.0.0
NEW=b259211157ec62299d7e2fad005827979d087b21   # tag v4.0.1

seed_beliefs() {
    # Evidence lines verified against the pinned v3.0.0 tree.
    belief b70e143d1d79375fbe346d9bb9ec5756bed4ee19 "PR #483: allow nil UUID (regex widened)" \
        "z.string().uuid() rejects the all-zero nil UUID (version digit must be 1-5)" \
        --evidence src/types.ts:413-413
    belief 85928549a7c2aff851d8625588ed7aa2e394831f "Zod 4 merge (#4074); v4 migration guide: .errors removed, use .issues" \
        "ZodError exposes an .errors getter aliasing .issues" \
        --evidence src/ZodError.ts:128-128
    belief 85928549a7c2aff851d8625588ed7aa2e394831f "Zod 4 merge (#4074): src/ tree replaced by packages/ monorepo" \
        "zod's core implementation is the single file src/types.ts" \
        --evidence src/types.ts:1-2
    # Control: .editorconfig is untouched across the whole range.
    belief SURVIVES ".editorconfig unchanged v3.0.0 -> v4.0.1" \
        "the repo pins editor conventions in .editorconfig" \
        --evidence .editorconfig:1-2
}
