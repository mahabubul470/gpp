# go-redis v8.0.0 -> v9.0.0 (388 first-parent commits).
# Culprits map to the v9 migration notes: pool options renamed
# (MaxConnAge -> ConnMaxLifetime, #2171), the Hook interface redesign
# (#2244), and Pipeline.Close removed at the v9 branch merge.
NAME=go-redis
URL=https://github.com/redis/go-redis.git
OLD=f354306eec06b425c8c0ce2ff9fa1971e467141f   # tag v8.0.0
NEW=dcb9e99533852dc35e081c9666d153bc7213ce92   # tag v9.0.0

seed_beliefs() {
    # Evidence lines verified against the pinned v8.0.0 tree.
    belief 3d1e2e5bc8af1bfec675334a9a3f4cd9919dfda3 "PR #2171 pool-options cleanup; v9 migration: MaxConnAge -> ConnMaxLifetime" \
        "the connection max-lifetime option is Options.MaxConnAge" \
        --evidence options.go:89-89
    belief 180f107add989d56d34734f1d429a83c6d4b9b91 "PR #2244 hooks redesign; v9 migration: new Hook interface" \
        "hooks implement BeforeProcess/AfterProcess on the Hook interface" \
        --evidence redis.go:22-24
    belief 0aa94538ee6bdc8eeb79bbd50edb82f87b0e373e "v9 branch merge; Pipeline.Close removed in v9" \
        "Pipeline has a Close method releasing its resources" \
        --evidence pipeline.go:71-71
    # Control: the redis.Nil sentinel re-export is untouched across the range.
    belief SURVIVES "const Nil = proto.Nil unchanged v8.0.0 -> v9.0.0" \
        "a missing key is reported as the redis.Nil sentinel error" \
        --evidence redis.go:14-14
}
