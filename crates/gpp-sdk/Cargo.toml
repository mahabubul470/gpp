[package]
name = "gpp-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "gpp agent SDK — Tier 3 native agent integration"

[dependencies]
gpp-core = { path = "../gpp-core", version = "0.2.2" }
gpp-timeline = { path = "../gpp-timeline", version = "0.2.2" }
gpp-history = { path = "../gpp-history", version = "0.2.2" }
gpp-graphex = { path = "../gpp-graphex", version = "0.2.2" }
gpp-cost = { path = "../gpp-cost", version = "0.2.2" }
gpp-notify = { path = "../gpp-notify", version = "0.2.2" }
gpp-rbac = { path = "../gpp-rbac", version = "0.2.2" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
